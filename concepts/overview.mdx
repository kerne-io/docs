---
title: 'How Kerne works'
sidebarTitle: 'How Kerne works'
description: 'What Kerne holds, and how a decision gets made'
---

Kerne sits between two things that change at different speeds: **what you sell**, which changes whenever the business decides it should, and **what your product does**, which you would rather not redeploy every time.

So Kerne holds the part in the middle — the rules that connect a customer's commercial situation to what they can actually do — and answers questions about them.

## What Kerne holds

Four kinds of rule, and they compose:

<CardGroup cols={2}>
  <Card title="What you sell" icon="tags">
    Your catalog: the plans, the prices, and what each plan includes. Configured once, versioned every
    time you change it.
  </Card>
  <Card title="What each customer bought" icon="receipt">
    Their subscription, the version of your offer it's pinned to, and its current state — active,
    past due, canceling at the end of the period.
  </Card>
  <Card title="What they've used" icon="gauge">
    Counters for anything with an allowance: how much this period, against what limit, and what
    happens once it runs out.
  </Card>
  <Card title="What was agreed with them specifically" icon="user-pen">
    Overrides — the negotiated limit, the goodwill credit, the deal that exists for exactly one
    customer and isn't part of your catalog.
  </Card>
</CardGroup>

None of that lives in your application. Your application asks.

## How a decision gets made

Your app names a **capability** — `export_pdf`, `api_calls`, `support_level` — and who it's asking about. Kerne resolves the rules that apply to that person, in this order:

```
Feature asked about
  → the subject's subscription (or your default plan, if they have none)
  → the pack that subscription is pinned to — the version of your offer they bought
  → the entitlement for that feature in that pack
  → an override, if one exists for this subject
  → their usage counter, for anything with an allowance
  → the answer
```

Which comes back as one of three things, depending on what you asked for:

| | Hands back | Use it for |
| --- | --- | --- |
| [`allows()`](/sdks/server/entitlements/allows-enforce-check) | `true` / `false` | The common case: gate an action |
| [`check()`](/sdks/server/entitlements/allows-enforce-check) | the whole picture — limit, used, remaining, why | Showing the customer where they stand |
| [`consume()`](/sdks/server/entitlements/consume) | the same, after counting the usage | When the action *is* the thing being metered |

The point isn't the shape of the call. It's that the answer changes when your pricing changes, when a customer upgrades, when a payment fails, or when you grant someone an exception — and none of those events touch your code.

## Who a decision is about

Every decision is about someone. In Kerne that's the **subject** — the SDK's `subject` parameter, and the thing an entitlement, a subscription, and a usage counter all belong to.

Today a subject is always one user. That fits products where individual people subscribe and are gated individually: a design tool where every seat logs in and pays, a ticketing SaaS charging per agent, an API sold per developer.

It's not a native fit yet if the thing you bill is **a group as one unit** — one company, twenty employees, one shared subscription, one pooled allowance. The usual workaround is to make one user (the account owner, the billing contact) the subject the subscription hangs on, and keep membership in your own database. That works, and it's a workaround you own rather than something Kerne represents. See [Is Kerne a fit?](/guides/is-kerne-a-fit).

<Note>
  Separately, your whole integration is one Kerne **tenant** — the boundary around your account. You'll
  only ever have one, and it isn't something you configure day to day.
</Note>

## Where to go next

<AccordionGroup>
  <Accordion title="You want to see the pieces">
    [Pricing lives outside your code](/concepts/monetization) walks the catalog: products, plans,
    prices, and the versioning that keeps existing customers on the deal they bought.
  </Accordion>
  <Accordion title="You want to gate something">
    [Entitlements](/concepts/monetization/entitlements) covers the three kinds of capability
    (on/off, an allowance, a config value) and what a check hands back.
  </Accordion>
  <Accordion title="You want to charge for consumption">
    [Usage billing](/concepts/monetization/usage-billing) covers rates, spending caps, and
    counting per project or per machine instead of per account.
  </Accordion>
  <Accordion title="You need users too">
    [Users and sessions](/concepts/identity) covers sign-up, log-in, and who is allowed to register.
    Already have an identity provider? You can keep it and mirror your users into Kerne — see
    [Is Kerne a fit?](/guides/is-kerne-a-fit) for what that costs you.
  </Accordion>
</AccordionGroup>

## What isn't built yet

Being explicit about this is worth more than a longer feature list. None of these exist today:

- **Organizations and teams** as the thing you bill — see [Who a decision is about](#who-a-decision-is-about).
- **Role-based access control** — no custom roles, no per-resource permissions. Admin operations use your secret key, not a user's session.
- **OAuth and social login** — email + password and magic link only.

The full discussion, including what people do instead, is in [Is Kerne a fit?](/guides/is-kerne-a-fit).
