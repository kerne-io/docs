---
title: "List public plans"
api: "GET /v1/pricing/plans/:productIdOrSlug"
---

Retrieve a list of active, public plans for a specific product. This endpoint is public and does not require authentication, but does require `x-app-id` (tenant header).
