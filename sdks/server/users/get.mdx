---
title: "Get user"
sidebarTitle: "Get user"
description: "Retrieve a user's full profile"
---

```typescript
const user = await kerne.users.get(userId);
```

Only succeeds for the user's own id, unless the caller authenticates with your secret key - see [Authorization](/concepts/identity/authorization).

## Response

<ResponseField name="id" type="string" required />
<ResponseField name="email" type="string" required />
<ResponseField name="email_verified" type="boolean" required />
<ResponseField name="first_name" type="string | null" />
<ResponseField name="last_name" type="string | null" />
<ResponseField name="avatar" type="string | null" />
<ResponseField name="is_active" type="boolean" required />
<ResponseField name="is_blocked" type="boolean" required />
<ResponseField name="created_at" type="string" required />
<ResponseField name="last_login_at" type="string | null" />
